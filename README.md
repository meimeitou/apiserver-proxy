# apiserver-proxy

```shell
xcaddy build \
  --with github.com/meimeitou/apiserver-proxy

# local build
xcaddy build \
  --with github.com/meimeitou/apiserver-proxy=./

./caddy list-modules

./caddy run --config example/Caddyfile
```
